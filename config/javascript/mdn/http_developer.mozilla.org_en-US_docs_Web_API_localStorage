The read-only <strong><code>localStorage</code></strong> property allows you to access a <a href="/en-US/docs/Web/API/Storage" title="The Storage interface of the Web Storage API provides access to a particular domain's session or local storage. It allows, for example, the addition, modification, or deletion of stored data items."><code>Storage</code></a> object for the <a href="/en-US/docs/Web/API/Document" title="The Document interface represents any web page loaded in the browser and serves as an entry point into the web page's content, which is the DOM tree."><code>Document</code></a>'s origin; the stored data is saved across browser sessions.