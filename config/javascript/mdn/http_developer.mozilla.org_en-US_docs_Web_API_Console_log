The <a href="/en-US/docs/Web/API/Console" title="The Console object provides access to the browser's debugging console (e.g. the Web Console in Firefox). The specifics of how it works varies from browser to browser, but there is a de facto set of features that are typically provided."><code>Console</code></a> method <strong><code>log()</code></strong> outputs a message to the web console.